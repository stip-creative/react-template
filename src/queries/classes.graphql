query Classes($course_type: String, $subject: String) {
    sidebar: allCourses(where: { course_type: $course_type, subject: $subject }) {
        edges {
            node {
                meta: _meta {
                    uid
                }
                course_type
                subject
                class
            }
        }
    }
}
